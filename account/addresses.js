const list=document.getElementById('address-list');document.getElementById('add-address')?.addEventListener('click',()=>alert('Test only: the add-address form will be connected to Supabase later.'));list?.addEventListener('click',event=>{const target=event.target;if(target.classList.contains('edit-address'))alert('Test only: address editing will be connected to Supabase later.');if(target.classList.contains('delete-address')){if(confirm('Remove this test address?'))target.closest('.address-card')?.remove();}if(target.classList.contains('set-default')){document.querySelectorAll('.address-card').forEach(card=>card.classList.remove('default'));document.querySelectorAll('.default-badge').forEach(badge=>badge.remove());const card=target.closest('.address-card');card?.classList.add('default');const label=card?.querySelector('.address-label');if(label){const badge=document.createElement('span');badge.className='default-badge';badge.textContent='Default';label.after(badge)}}});